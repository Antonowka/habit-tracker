import { MyHabitsList } from '../../interface/interface';

export function createBody() {
    const body: MyHabitsList = [
        {
            name: '',
            goal: 0,
            data:[''],
        },
    ];
    return body;
}
